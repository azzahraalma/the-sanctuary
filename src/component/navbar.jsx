export default function Navbar() {
  return (
    <nav className="navbar">

      <div className="logo">
        Beranda
      </div>

      <ul className="nav-links">
        <li>Mentor</li>
        <li>Dashboard</li>
      </ul>

      <button className="mentor-btn">
        Temukan Mentor
      </button>

    </nav>
  )
}